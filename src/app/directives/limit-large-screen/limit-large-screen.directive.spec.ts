import { LimitLargeScreenDirective } from './limit-large-screen.directive';

describe('LimitLargeScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitLargeScreenDirective();
    expect(directive).toBeTruthy();
  });
});
